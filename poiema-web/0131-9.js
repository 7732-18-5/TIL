// 삼각형출력하기 
// 다음을 참고하여 * (별)로 높이가 5인(var line = 5) 삼각형을 문자열로 완성하라.
// 개행문자(‘\n’) 를 사용하여 개행한다.
// 완성된 문자열의 마지막은 개행문자(‘\n’)로 끝나도 관게없다.
// 높이(line) : 5
var asterisk = '';
var tri = '';
for (var i = 0; i < 5; i++) {
    asterisk += '*';
    tri += asterisk + '\n';
}
    console.log(tri);
// 
do {
    console.log('asterisk')
}
// 
var asterisk = '';

for (var i = 0; i < 5; i++) {
    for (var j = 0; j < 5; j++) {
        asterisk += '\n';
            console.log(\n)
        }
    }
}
//
    
var asterisk = '*'

for (var i = 1; i <= 5; i++) {
    asterisk += '\n' * i;
    console.log(asterisk)
}

var asterisk = '';
while (var asterisk='*')


// 

var star = '';

for (var i = 0; i < 5; i++) {
    // i = 0 , * 1개
    // i = 1 , * 2개
    // i = 2 , * 3개
    // console.log('*');
    // console.log는 자동으로 개행한다.
    for (var j = 0; j <= i; j++) {
        star += '*';
    }
    star += '\n';
}